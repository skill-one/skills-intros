---
description: "黑盒视角的功能与输入输出介绍, 不谈内部实现"
output: BlackBoxIntro
---

请站在纯使用者的角度介绍你自己, 把你当作一个不透明盒子: 只描述从外面能看到的行为, 完全不要提内部实现、执行步骤、文件结构或依赖了什么工具/库。

请输出:
1. function: 用一句话说清这个 skill 是做什么的。
2. input_output: 列出 3~5 条典型的「输入 → 输出」对照, 每条格式为「输入 X → 输出 Y」。输入侧写用户实际会给出什么 (如文件、URL、一段文本、一条命令), 输出侧写用户实际会拿到什么 (如文件、报告、修改后的代码)。示例:
   - 输入 .md 文件路径 → 输出排版好的 PDF 文件
   - 输入一段中文文本 → 输出对应的英文译文

要求: 只写盒子外面能观察到的行为; 输入输出要具体、可举例, 不要写成「处理各种输入并给出结果」这类空话。
