# firebase-firestore (`firebase/agent-skills/firebase-firestore`)

## comments

- user: 全栈独立开发者, category: 妙用, comment: 项目里 Standard 和 Enterprise 两个库并存, 它会先列出所有库让我挑, 不默认操作第一个. 多库项目把库名直接报给它最省事.
- user: 第一次接 Firebase 的新手, category: 坑, comment: 以为它能顺手配好登录和文件存储, 结果只管 Firestore 数据库, Auth、Storage 明确不碰. 这类需求要另外安排, 别指望一个技能全包.
- user: Python 后端, category: 注意, comment: 让它写 Enterprise 的 Python 代码前, 它强制先读对应平台的参考文档. 别嫌慢催它跳过, Enterprise 的初始化写法和网上常见教程不一样, 跳过就跑不通.
- user: 运维老哥, category: 注意, comment: 新建库时 location 选完基本改不了. 它会列出全部可选位置并建议和现有资源同区域, 这步别一路回车, 先确认业务到底在哪个区域再定.
- user: Flutter 独立开发者, category: 妙用, comment: 本机不装 Firebase CLI 也行, 它全程用 npx 每次拉最新版执行, 不会再碰上"CLI 版本太旧"这种扯皮. 新电脑零配置直接开工.
- user: 小团队技术负责人, category: 启发, comment: 它第一步永远是先列库、确认版本、再问我要操作哪个, 而不是直接动手. 我后来让团队写运维脚本也照这个来: 先探明环境再执行, 返工少多了.
