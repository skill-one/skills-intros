# dbs-agent-migration (`dontbesilent2025/dbskill/dbs-agent-migration`)

## scenario

你同时用 Claude Code、Codex、Grok,skill 各存一份:改了这边忘了那边,不知哪份是真源,`/` 调不动,越攒越乱。我帮你一次理清:审计现状、定唯一真源、统一命名、生成多端 bridge,以后只改一处,多端一致。🧹
