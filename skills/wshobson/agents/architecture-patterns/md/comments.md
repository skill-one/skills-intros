# architecture-patterns (`wshobson/agents/architecture-patterns`)

## comments

- user: 十年后端老兵, category: 妙用, comment: 我拿它当分层体检工具：跑一遍业务用例测试，凡是必须连数据库才能通过的，就是逻辑漏进底层了，照此反推修补，比人工读代码快得多。
- user: 第一次带项目的新手, category: 注意, comment: 先想清模块边界再问：它是在「给定模块」上做分层设计，不会替你划边界。我只说「帮我设计架构」时产出很泛，列清模块和外部依赖后立刻能用。
- user: 拆单体失败过的技术负责人, category: 注意, comment: 别急着拆服务，先让它帮你划上下文。我曾让订单和用户共用一套实体，拆开后改个字段两边都炸；改成各自轻量模型、只走接口调用才真正解耦。
- user: 被测试环境拖垮的中级开发, category: 妙用, comment: 让用例测试彻底不碰真库：在内存里造假仓库传给用例，秒级跑完、免 Docker。前提是用例只认抽象接口，直接传具体类这招就失灵。
- user: ORM 重度用户, category: 坑, comment: 图省事把数据库字段定义直接挂在业务模型上，结果换库时满屏报错。后来让它在旁边生成独立的存储映射对象，两边互转，业务模型才干净。
- user: 被循环依赖折磨的架构师, category: 启发, comment: 「规则层只准引用领域层」这条红线点醒我：分层不是束缚，是给依赖画的单行道。全组执行后，循环导入从每周一报变成绝迹。
