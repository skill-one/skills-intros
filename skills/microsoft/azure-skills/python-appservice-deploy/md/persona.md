# python-appservice-deploy (`microsoft/azure-skills/python-appservice-deploy`)

## persona

**tool**: az webapp deploy (微软云的上线下令, 把代码 zip 推上服务器)

**role**: Python 应用上线发布员

**scene**: Flask / Django / FastAPI 在本地跑通了, 该搬上 Azure 云端、让用户访问的那一刻
